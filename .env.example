# BUDDY 2.0 Environment Configuration Template
# Copy this file to .env and update with your actual credentials

# Core Backend Configuration
BUDDY_HOST=localhost
BUDDY_PORT=8082
BUDDY_SYNC_PORT=8083
BUDDY_API_PORT=8081

# Frontend/Client Configuration  
REACT_APP_BUDDY_HOST=localhost
REACT_APP_BUDDY_PORT=8082
REACT_APP_BACKEND_URL=http://localhost:8082

# Security Configuration
CLIENT_TOKEN=your-shared-secret-token-here
ADMIN_API_KEY=your-admin-api-key-here

# Pinecone Vector Database Configuration
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_INDEX=quickstart
USE_VECTOR_MEMORY=1

# MongoDB Atlas Configuration
MONGODB_URI=mongodb+srv://username:password@cluster0.fbbl9jd.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
USE_MONGODB=1
MONGODB_DATABASE=BUDDY_DB
MONGODB_TIMEOUT=10000

# BUDDY Settings
BUDDY_ENV=development
BUDDY_DEBUG=1
BUDDY_JWT_SECRET=generate-secure-64-byte-secret-for-production
ACCESS_TOKEN_EXPIRY=15m
REFRESH_TOKEN_EXPIRY=7d

# Weather API (Optional)
WEATHER_API_KEY=your_weather_api_key_here

# OpenAI Configuration (Optional)
OPENAI_API_KEY=your-openai-api-key

# Firebase Configuration (for realtime features)
FIREBASE_SERVICE_ACCOUNT_PATH=config/firebase-service-account.json

# Flutter/Dart Configuration (passed via --dart-define)
# Use these with: flutter run --dart-define=BUDDY_HOST=localhost --dart-define=BUDDY_PORT=8082
# BUDDY_HOST=localhost
# BUDDY_PORT=8082

# Production Configuration Examples:
# BUDDY_HOST=0.0.0.0
# BUDDY_PORT=80
# BUDDY_SYNC_PORT=8083
# REACT_APP_BUDDY_HOST=your-domain.com
# REACT_APP_BUDDY_PORT=80
# REACT_APP_BACKEND_URL=https://your-domain.com
# BUDDY_ENV=production
# BUDDY_DEBUG=0
# USE_MONGODB=1
# USE_VECTOR_MEMORY=1

# Local Development Examples:
# BUDDY_HOST=localhost
# BUDDY_PORT=8082
# BUDDY_ENV=development
# BUDDY_DEBUG=1
# USE_MONGODB=0  # Use JSON fallback for local testing
# USE_VECTOR_MEMORY=1

# Cross-Platform Deployment Notes:
# - Set BUDDY_HOST=0.0.0.0 for external access
# - Use specific IP address for LAN access (e.g., BUDDY_HOST=192.168.1.100)
# - Production deployments should use HTTPS with reverse proxy
# - Mobile apps will need to connect to actual IP/domain, not localhost
# - Docker deployments should expose appropriate ports